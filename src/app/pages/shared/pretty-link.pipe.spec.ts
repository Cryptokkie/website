import { PrettyLinkPipe } from './pretty-link.pipe';

describe('PrettyLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new PrettyLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
